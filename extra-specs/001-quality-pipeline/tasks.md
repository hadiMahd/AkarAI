# Tasks: Quality Pipeline

- [X] Add `pre-commit` configuration and repo hook setup.
- [X] Add deterministic CI workflows for backend, frontend, admin, worker, and model-service checks.
- [X] Standardize local quality commands so CI and local runs use the same entry points.
- [X] Add RAG evaluation workflow wiring with explicit opt-in for live/provider-backed runs.
- [X] Document what is blocking in CI versus what is optional/manual.
- [X] Validate the pipeline in Docker/local execution paths already used by this repo.
