# Tasks: Quality Pipeline

- [ ] Add `pre-commit` configuration and repo hook setup.
- [ ] Add deterministic CI workflows for backend, frontend, admin, worker, and model-service checks.
- [ ] Standardize local quality commands so CI and local runs use the same entry points.
- [ ] Add RAG evaluation workflow wiring with explicit opt-in for live/provider-backed runs.
- [ ] Document what is blocking in CI versus what is optional/manual.
- [ ] Validate the pipeline in Docker/local execution paths already used by this repo.
